pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("${settings.rootDir}/.flutter-plugins") } // 👈 required for Flutter plugin
    }

    plugins {
        id "dev.flutter.flutter-gradle-plugin" version "1.0.0"
    }
}
includeBuild("/Users/huzaifa/flutter_3.32.8/packages/flutter_tools/gradle")
include(":app")
